feat: Add logout icon and last login time display

Implemented a logout icon positioned at the top right of the container, which appears after successful login. Added functionality to display the last login time at the bottom of the page, persisting across sessions and clearing on logout.